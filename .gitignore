# Pastas que NÃO devem ir para o servidor
_backup-site-claro/
prints-sistemas/
EvaCloudd-Arquivos-Privados/
node_modules/
*.log
.claude

# Token da Conversions API (Meta) — só existe no servidor, nunca no Git
api/config.php
