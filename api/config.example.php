<?php
if (!defined('EVA_API')) { exit; }

/**
 * Copie este arquivo para "config.php" (mesma pasta) e preencha os valores.
 * config.php NUNCA deve ir para o Git (já está no .gitignore) — só para o servidor.
 */

// ID do Meta Pixel (o mesmo que está em assets/js/main.js -> CONFIG.metaPixelId)
define('META_PIXEL_ID', '');

// Token de acesso da Conversions API (gerado em Eventos Manager > Configurações > Conversions API)
define('META_ACCESS_TOKEN', '');
